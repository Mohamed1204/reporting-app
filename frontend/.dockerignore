# Dependencies — reinstalled inside the image via `npm ci`
node_modules/

# Build output — regenerated by `npm run build`
dist/

# Test artifacts and local junk
cypress/screenshots/
cypress/videos/
*.log
.git

# Docker files don't belong inside the image
Dockerfile
.dockerignore
